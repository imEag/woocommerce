import React from "react";
import styled from "styled-components";

const StyledCta = styled.div`
    
`;

export const Cta = () => {
    return (
        <StyledCta>
            
        </StyledCta>
    );
};