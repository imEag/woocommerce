import React from "react";
import styled from "styled-components";

const StyledHeader = styled.div`
    
`;

export const Header = () => {
    return (
        <StyledHeader>
            
        </StyledHeader>
    );
};