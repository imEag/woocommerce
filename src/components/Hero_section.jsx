import React from "react";
import styled from "styled-components";

const StyledHero = styled.div`
    
`;

export const Hero = () => {
    return (
        <StyledHero>
        </StyledHero>
    );
};