import React from "react";
import styled from "styled-components";

const StyledPeople = styled.div`
    
`;

export const People = () => {
    return (
        <StyledPeople>
            
        </StyledPeople>
    );
};